# Example environment variables used by the project (no secrets here).
# Copy to `.env` locally and fill values that your local dev or server needs.

# Firebase (if using client SDKs, these are usually safe public config values)
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_DATABASE_URL=
FIREBASE_PROJECT_ID=

# Server-specific
PORT=3000
